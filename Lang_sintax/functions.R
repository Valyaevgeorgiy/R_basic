

# функция обычно возвращает последнюю выполненную строку в теле функции 
pow <- function(x, y){
  print(x ^ y)
}
  
pow(2, 5)

4 + 3 * pow(2, 5)


po2 <- function(x, y) {
  return(x ^ y)
  z1 <- 2 + 2
  z2 <- "Пора домой"
}

w <- 3 * po2(3, 2) + 12
w